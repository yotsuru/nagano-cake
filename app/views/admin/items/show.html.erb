<h3>商品詳細</h3>
 
  <div class="row">
   <div class="col-lg-4">
   <%= image_tag 'no_image', size: "280x230" %>
   </div>

   <div class="col-lg-8">
     <h4>商品名</h4>
     <p><%= @item.name %></p>

     <h4>商品説明</h4>
     <p><%= @item.introduction %></p>

     <h4>税込価格(税抜価格)</h4>
     <p><%= (@item.price*1.1).floor.to_s(:delimited) %>(<%= (@item.price).floor.to_s(:delimited) %>)円
   
     <%= link_to "編集する",edit_admin_item_path(@item), class: "btn btn-success" %>
   </div>
  </div>